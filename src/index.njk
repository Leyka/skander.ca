---
title: Hello world!
layout: 'base.njk'
---

<h2>Articles</h2>
<ul>
  {% for article in collections.articles %}
    <li><a href="{{ article.url }}">{{ article.data.title }}</a> ({{ article.date | formatDate }})</li>
  {% endfor %}
</ul>
